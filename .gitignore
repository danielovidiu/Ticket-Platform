# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Supabase local dev
supabase/.branches
supabase/.temp

# Symlink to a separate project with its own git repo & remote
# (danielovidiu/Ticket-Platform). The real checkout lives at
# ~/Developer/Ticket-Platform — it cannot live under ~/Documents because that
# subtree is TCC-protected and dev servers there fail on getcwd().
/Ticket-Platform

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
